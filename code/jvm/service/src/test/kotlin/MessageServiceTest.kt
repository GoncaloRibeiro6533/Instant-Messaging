import kotlin.test.assertEquals
import kotlin.test.assertIs

class MessageServiceTest {
/*todo
    @Test
    fun `Test to get messages history` () {
        val user = userService.createUser("user", "password", "email@email.com")
        assertIs<Success<User>>(user)
        val channel = channelService.createChannel("channel", user.value.id, Visibility.PUBLIC)
        assertIs<Success<Channel>>(channel)
        val limit = 5
        val skip = 5
        val result = channelService.getMsgHistory(channel.value.id, limit, skip)
        assertIs<Success<List<Message>>>(result)
        assertEquals(channel.value.messages, result.value)
    }

        @Test
    fun `Test getMsgHistory with negative channel ID`() {
        val exception = channelService.getMsgHistory(-1)
        assertIs<Failure<ChannelError>>(exception)
        assertEquals(ChannelError.NegativeIdentifier, exception.value)
    }

 */
}